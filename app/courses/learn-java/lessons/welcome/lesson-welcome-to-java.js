export const lessonWelcomeToJava = {
  id: "welcome-to-java",
  title: "Welcome to Java",
  hasChallenge: false,
  article: `
## Welcome to Java

Before you write a single line of code, there's something more important to understand:

**what Java actually is, and why it matters.**

Java is one of the most widely used programming languages in the world — not because it's trendy, but because it's been solving real problems at scale for decades.

This article is your starting point.
No syntax. No pressure. Just clarity.

---

## What Is Java?

Java is a **statically-typed, compiled, object-oriented programming language** first released in 1995 by Sun Microsystems (now owned by Oracle).

It was designed around one powerful idea:

> *Write once, run anywhere.*

Java code doesn't compile directly to machine code for a specific processor. Instead, it compiles to **bytecode** that runs on the **Java Virtual Machine (JVM)** — a runtime that exists on nearly every platform: Windows, macOS, Linux, Android, and more.

This means a Java program written on a Mac can run unchanged on a Windows server. That's rare. That's powerful.

---

## What Can You Build With Java?

Java is used in industries that power the modern world:

- **Android apps** — The Android platform is built on Java (and Kotlin, which runs on the same JVM)
- **Enterprise backends** — Banks, insurance companies, airlines, and hospitals run Java at their core
- **Web APIs and microservices** — Spring Boot is the most popular backend framework in enterprise software
- **Data engineering** — Apache Kafka and Apache Spark are written in Java/Scala on the JVM
- **Developer tooling** — Build systems like Gradle and Maven are Java applications
- **Financial systems** — High-frequency trading systems rely on Java's performance and predictability

Java is not a toy language for learning. It's the language of systems that cannot afford to fail.

---

## What You Will Build in This Course

This course is structured around real, working programs — not exercises that disappear after the lesson ends.

By the time you finish, you will have built:

- A command-line Java application with user input and a working loop
- A set of reusable OOP classes modeling real-world entities
- A program that reads and writes files
- A tool that calls a real API and parses JSON responses
- A tested codebase with JUnit tests
- A capstone project hosted on GitHub

These are not practice problems.
These are proof of your ability.

---

## What Comes Next

The next lesson answers a question that trips up many beginners:

**What does programming actually mean?**

Before you touch Java syntax, you need to understand how computers execute instructions — literally, one at a time, no assumptions, no guessing.

When you're ready, continue to:
**0.2 What Programming Actually Is**
`,
};
